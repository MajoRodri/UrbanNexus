services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:////app/data/clima.db
    volumes:
      - db_data:/app/data
    restart: unless-stopped

volumes:
  db_data:
